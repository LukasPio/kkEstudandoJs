let x = window.prompt("Digite o primeiro valor")
x = parseFloat(x)
let y = window.prompt("Digite o segundo valor")
y = parseFloat(y)
let soma = x + y
let subtra = x - y
let mult = x * y
let division = x / y
alert("Os resultados são:" +
  "\nSoma: " + soma +
  "\nSubtração: " + subtra +
  "\nMultiplicação: " + mult +
  "\nDivisão: " + division)